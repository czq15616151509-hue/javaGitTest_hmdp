待学习内容：
1.mybatis plus和springcache的学习和使用


主要知识点和重要内容：
（登录功能1-2，多tamcat情况下怎么共享）
1.拦截器里面重写了后处理：处理完请求之后统一销毁threadlocal空间，防止内存泄漏。
2.使用redis来实现session共享，满足多tomcat服务器的session一致性要求，就是登录
验证时，把验证码、用户信息等不在存入session而是redis中。
（商品查询3，三种redis问题）
3.如何解决redis的缓存穿透、缓存雪崩、缓存击穿问题(在ShopServiceImpl类代码中学习)
（秒杀卷）（基本功能实现，多tamcat情况下实现分布式锁解决并发问题）（异步秒杀优化实现：提高性能）
4.全局ID生成器：使用数据库自增存在安全问题(订单号是需要返回给用户的，用户可以推算出你的订单量)以及唯一性问题(删除订单的订单号可以会重复生成)，
此时我们的方案是借用redis里面的自增功能（唯一性、高性能），安全问题的解决方案是，我们在不直接使用自增的序列号作为订单号，而且会在自增的序列号
前面还要添加一个时间戳前缀，拼接起来作为订单号。
5.分布式锁：(实现一人一单)
    实现方式的选择：实现方式有mysql、redis和zookeeper。mysql是利用其本身互斥锁机制，redis使用其setnx的互斥指令，zookeeper利用临时节点的唯
                 一性和有序性（按照所有人的临时节点顺序大小获取锁）。三者的可用性都是没问题的，但是mysql性能和zookeeper性能都不如redis，在安
                 全性方面mysql断开会自动释放锁，zookeeper创建的是临时节点，断开也会自动释放锁，redis则需要利用超时时间，到期释放，安全性虽然
                 不敌前两者，但是均衡考虑业务的需求，redis更符合业务需求。而且我们使用lua脚本来保证（变量判断+变量修改）的原子性，避免阻塞带来
                 的并发问题。
    介绍强大的分布式锁工具redisson：该组件里面有满足可重入、可重试、redis集群等各种不同的锁(底层其实还是redis，方法已经被封装好了)。
    redisson基础：
           可重入锁原理：底层redis使用hash结构来实现，既要在锁中绑用户线程信息，也要绑重入次数。
           可重试锁原理：依据等待时间不断重试获取锁，等待时间过期则不继续重试；如果没有设置等待时间，默认是持续等待，底层设置了定时任务，定
           时执行任务————刷新过期时间；代码底层巧妙使用了消息订阅机制，锁被释放后会发出通知线程来重新获取锁。

    最终技术选择：redis+lua组合适合，能满足小企业需求，成本低；redisson适合大企业需求，毕竟多加一个中间组件需要成本。但是redisson功能更强大。
           本项目两种方式都有实现，前者参考学习，学习底层原理，但后续代码都是建立在使用redisson提供的分布式锁功能基础上实现的。
6.异步秒杀优化：redisson+异步。秒杀资格判断，与秒杀订单的生成及存储实现异步处理。
    实现思路：在添加秒杀卷的时候，要把秒杀卷信息同步到redis中，还要把库存也同步到redis中，以及每生产一个秒杀订单后也要同步到redis中。我们在redis
            中对秒杀订单资格进行判断，判断成功就认为已经成功秒杀，后续操作交给异步线程处理。
    异步秒杀的实现方式：
        消息队列的选择：
            使用jdk自带的阻塞队列；（存在数据安全问题和内存受限问题）
            使用redis中的List结构模拟消息队列，BRPOP和BLPOP来完成阻塞功能；（只支持单消费者）
            基于PubSub的消息队列；（支持多生产者多消费者，而且消费者可以订阅多个通道，但是不支持数据持久化，而且超时时也会造成数据丢失）
            (推荐)基于Stream的消息队列；（是一种功能非常完善的消息队列，$指每次读新消息，如果一次性塞进多条消息就会导致漏读，但是可以使用消费者
            组来避免这种问题）
        基于消费者组的Stream消息队列：将多个消费者划分到一个组中共同监听同一个队列。
                特点1，消息分流，加快消息处理；
                特点2，同一个消费者组共同维护一个消息标识，该标识记录最后一个被处理的消息，就算宕机重启也能保证每一个消息被消费；
                特点3，消息确认，被消费者拿走的消息会处于pending-list中，处理完之后回复XACK进行确认，标记为已处理才会从该list中去除。
                优势：消息可回溯；多消费者竞争加快消费；没有漏读；有确认机制。
7.

易错内容：
1.依赖注入会报bean创建失败的错误原因：某个类的创建是被spring创建的，那么它的
属性才能被依赖注入。所以InterceptorRefreshToken要被spring容器管理才能进行
StringRedisTemplate的依赖注入。


自己对项目的改造：一个是改造朋友圈（使用推拉结合达到千万用户级别）；第二个改造的点是查询附近店铺的时候，多传进来一个查询范围参数，1km范围内或5km范
               内等，默认是三公里（可选择距离）。

